Todo API berhasil di-deploy!

Akses aplikasi:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  http://{{ $host.host }}
{{- end }}
{{- else }}
  kubectl port-forward svc/{{ include "web-app.fullname" . }} 8000:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  Lalu buka http://localhost:8000
{{- end }}

Cek status pod:
  kubectl get pods -n {{ .Release.Namespace }}
